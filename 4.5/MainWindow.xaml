<Window x:Class="_4._5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_4._5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="YellowGreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Введення ваги у фунтах -->
        <TextBox x:Name="PoundsTextBox" Width="150" Height="30"
                 Grid.Row="0" HorizontalAlignment="Center" Margin="0,10"/>

        <!-- Кнопка розрахунок -->
        <Button Content="Розрахунок" Width="120" Height="30"
                Grid.Row="1" HorizontalAlignment="Center" Margin="0,10"
                Click="ConvertButton_Click"/>

        <!-- Результат -->
        <TextBlock x:Name="ResultTextBlock"
                   Grid.Row="2"
                   HorizontalAlignment="Center"
                   FontSize="16" Margin="0,10"/>

    </Grid>
</Window>
